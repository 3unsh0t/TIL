//Minimum Increment to Make Array Unique
// 처음 짠 코드

class Solution {

    public int minIncrementForUnique(int[] nums) {
        // <945. 고유배열로 만드는 가장 작은 횟수를 구하라>
        // 32-비트 정수 배열이 주어지는데, 각 배열의 숫자가 고유한 숫자이도록 만들기 위해 1을 더하는 최소횟수를 구하는 문제
        
        int count = 0;

        // 배열 인덱스 1부터 검사한다
        for(int i=0; i<nums.length; i++){
            // 배열에 nums[i]를 제외하고 nums[i]와 같은 값을 가지는 요소가 있는지 검사한다.
            for(int j=0; j<nums.length; j++){
                // j 인덱스를 가지고 검사한다.
                if(j != i && nums[j]==nums[i]){
                    nums[i]++;
                    count++;
                    j = -1; //다시 0부터 검사(위에서 j++하므로)
                }
            }
        }
        return count;
    }

}
